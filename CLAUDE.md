# 공부한입 (StudyBite)

대학생용 AI 학습 도우미. 브랜드명 "공부한입" — 로고(`src/components/Logo.tsx`)는 책을 한입 베어문 모양(Apple 로고 패러디, SVG `<mask>`로 원 모양을 정확히 도려냄 — `fill-rule="evenodd"`로는 모서리를 물면 바깥으로 튀어나온 영역이 잘못 채워지는 버그가 남, 반드시 mask 방식 사용). 메뉴: **노트/요약**(`/notes`, 요약/설명 생성), **예상문제출력**(`/exams`, 참고자료·기출문제 파일 업로드로 시험 생성→응시→채점→취약점 분석), **AI선생님**(`/tutor`, 파일 기반 챗봇 — 노트 상세 페이지의 탭이 아니라 독립 메뉴), **PDF 영어자료 변환**(`/translate`, 영어 PDF를 DeepL Document Translation API로 원본 레이아웃 유지한 채 한글 PDF로 번역 — 노트/시험/AI선생님과 완전히 분리된 별도 파일 풀), **문의하기**(`/inquiries`, 전체 사용자 문의를 익명으로 볼 수 있는 게시판 — 등록 폼은 버튼 클릭 시에만 노출), **요금제**(`/billing`, 토스페이먼츠 자동결제 구독), 관리자 전용 **관리자**(`/admin/inquiries`, 네비게이션에 `ADMIN_EMAIL` 일치 시에만 노출, 작성자 식별+삭제 가능). 상세 아키텍처는 `README.md` 참고.

## 스택 / 버전 관련 주의사항

- **Next.js 16** (App Router, Turbopack). 학습 데이터 기준보다 최신이라 API가 다를 수 있음 — 확신이 없으면 `node_modules/next/dist/docs/`를 먼저 확인할 것.
  - 미들웨어 파일명이 `middleware.ts`가 아니라 **`src/proxy.ts`** (`export function proxy(request)`).
  - 동적 라우트 `params`는 항상 `Promise` — `await params` 필요 (page/layout/route 공통).
  - Route Handler의 컨텍스트 타입은 전역 헬퍼 `RouteContext<'/api/path/[id]'>` 사용.
- **Prisma 6.19** — 최신 `prisma-client` 제너레이터 사용 중 (구버전 `prisma-client-js` 아님). 생성된 클라이언트는 `node_modules`가 아니라 **`src/generated/prisma`**에 출력되며 `import { PrismaClient } from "@/generated/prisma/client"`로 임포트. `prisma.config.ts`가 `.env`를 명시적으로 로드함 (`import "dotenv/config"`).
  - SQLite 파일 경로는 **절대경로**로 `.env`의 `DATABASE_URL`에 지정되어 있음 (상대경로 `file:./dev.db`는 CLI와 런타임이 서로 다른 기준으로 해석해서 "Unable to open the database file" 에러가 났었음 — 절대경로로 우회).
- **Zod v4** — `.email()` 등 v3 스타일 체이닝 API 그대로 사용 가능 (classic API).
- **Anthropic SDK (`@anthropic-ai/sdk`)** — `anthropic.messages.parse({..., output_config: { format: zodOutputFormat(schema) }})`로 구조화 출력을 바로 Zod 타입으로 받음. 이미지/PDF는 vision 콘텐츠 블록(`type: "document"` for PDF, `type: "image"`)으로 직접 전송 — 별도 OCR/텍스트 추출 라이브러리 불필요 (PDF는 스캔본도 Claude가 직접 처리).
- **@react-pdf/renderer** — 한글 폰트로 **Nanum Gothic**(`public/fonts/`) 사용 중. Google Fonts의 Noto Sans KR은 가변 폰트만 배포되어 react-pdf(fontkit)와 호환 안 됨 → 정적 TTF를 제공하는 나눔고딕으로 대체함. `Font.register`의 `src`에 로컬 파일 경로 문자열을 주면 내부적으로 `fontkit.open()`으로 로드됨 (URL이면 `fetch`, data URI면 base64 디코딩) — `file://` URL은 Node fetch가 지원 안 하므로 절대 사용하지 말 것.
- **토스페이먼츠 (`@tosspayments/tosspayments-sdk` v2, `src/lib/toss.ts`)** — Stripe와 달리 SDK/REST API가 "구독(Subscription)" 객체를 제공하지 않음. `requestBillingAuth()`는 카드 등록만 하고 금액을 받지 않음(`amount`/`orderId`/`orderName`은 이 호출에 없음 — 실제 타입 정의로 확인함) — 첫 결제와 매달 재청구는 전부 서버에서 별도로 `POST /v1/billing/{billingKey}`를 호출해야 함. 웹훅(`/api/billing/toss/webhook`)도 `request.text()`로 raw body 필요(Stripe 때와 동일 이유), 서명 헤더는 `tosspayments-webhook-signature`(`v1:...` 형식, HMAC-SHA256(payload+":"+전송시각 헤더) 중 하나라도 일치하면 통과).

## 아키텍처 메모

- 인증: NextAuth v4 Credentials + JWT 세션. `getServerSession(authOptions)`는 서버 컴포넌트/라우트에서 `next-auth/next`로부터 임포트.
- 시험 문제의 정답/모범답안(`correctAnswer`/`modelAnswer`/`explanation`)은 `GET /api/exams/[id]`(응시용)에서는 절대 내려주지 않음 — 채점 후 `attempts/[attemptId]` 결과에서만 노출. 새 exam 관련 API를 추가할 때 이 경계를 유지할 것.
- 시험지 렌더링은 `src/lib/exam/formatters.ts`(문항 번호, 선택지 라벨, 답안 줄 수, 총점)를 웹뷰(`ExamPaperWeb`)와 PDF(`ExamPaperPdf`)가 공유 — 두 뷰가 갈라지지 않도록 새 포맷팅 로직은 여기에 추가.
- 파일 업로드는 `storage/uploads/{userId}/{uuid}-{name}`에 저장 (`src/lib/storage.ts`), DB에는 상대경로만 저장. 삭제 시 DB row와 디스크 파일을 함께 정리해야 함 (안 그러면 orphan 파일 발생 — 과거에 한 번 발생했던 버그).
- **파일 업로드는 노트/시험/AI선생님이 공유하는 단일 풀**: 모든 사용자 업로드는 `src/lib/upload.ts`의 `createUploadedFile()`을 거쳐 항상 `purpose: "note"`로 생성됨(과거엔 시험용 파일을 `exam_reference`/`exam_past_exam`이라는 별도 purpose로 만들고 `/api/exams/files`라는 전용 엔드포인트를 썼지만, 지금은 삭제됨 — 시험 생성도 `/api/notes` POST를 그대로 재사용). 업로드 엔드포인트는 `/api/notes` POST 하나뿐이고, `/notes`·`/tutor`·마이페이지 자료 보관함·`/exams/new`가 전부 같은 `purpose: "note"` 파일 목록을 각자 쿼리해서 보여줌 — **한 곳에서 올린 파일은 재업로드 없이 다른 세 곳에서도 바로 선택해서 쓸 수 있음**. 새 기능에서 파일을 업로드해야 하면 `/api/notes` POST를 재사용할 것(별도 purpose/엔드포인트를 새로 만들지 말 것).
- **지원 파일 형식**(`src/lib/fileKind.ts`): PDF, DOCX, PPTX, 이미지(png/jpeg/webp/gif). PPTX는 `src/lib/extract/pptx.ts`가 JSZip으로 zip 내부(OOXML)를 직접 열어 슬라이드 XML에서 텍스트를, `ppt/media/`에서 래스터 이미지(emf/wmf 등 벡터는 vision이 못 읽어서 제외)를 뽑아 씀 — LibreOffice 같은 네이티브 렌더러 없이 순수 JS로 처리(Windows 로컬/Vercel 서버리스 둘 다 안전, `@hyzyla/pdfium`을 쓴 이유와 동일한 제약). 슬라이드 순서는 파일명 숫자가 아니라 `presentation.xml`의 `sldIdLst`+`presentation.xml.rels`로 구함(파일명 번호는 슬라이드 추가/삭제 이력 때문에 실제 순서와 어긋날 수 있음). DOCX와 달리 업로드 시점엔 유효성 검증(+슬라이드 수 상한 체크)만 하고 텍스트/이미지를 DB에 저장하지 않음 — `buildFileContentBlocks`(`src/lib/claudeContent.ts`)가 매 호출마다 원본 pptx를 다시 열어 재추출(이미지까지 있어서 DOCX처럼 텍스트 하나만 캐싱하는 걸로 안 끝나고, 별도 이미지 저장 테이블/파일을 새로 만들면 orphan 파일 위험이 늘어나므로 원본 하나만 저장해두는 쪽을 택함). **HWP(한글)는 지원하지 않음** — 구버전은 한컴 독점 바이너리라 신뢰할 만한 순수 JS 파서가 없고, 제대로 하려면 유료 변환 API(사용자 자료를 제3자로 보내는 문제) 또는 네이티브 바이너리(서버리스와 안 맞음)가 필요해서 의도적으로 미지원 — 대신 `createUploadedFile()`이 파일명 확장자로 감지해서 "PDF로 내보내 주세요" 안내를 반환함(`isHwpFile()`).
- `UploadedFile.subjectId`(nullable, `Subject`에 대한 FK, 과목 삭제 시 `SetNull`)로 자료를 과목별로 정리함. 마이페이지(`/profile`)의 "자료 보관함"(`MaterialsLibrary.tsx`)이 과목별로 파일을 묶어 보여주고 업로드·재분류(`PATCH /api/notes/[id]`)·삭제를 제공하는 관리 화면이며, `/notes/new`·`/tutor`·`/exams/new`의 업로드 폼에도 동일한 `SubjectPicker`가 붙어 있어 어디서 올리든 과목이 붙음(미분류도 허용).
- **`/profile`("마이페이지")와 `/profile/account`("내 정보")는 서로 다른 페이지**: `/profile`은 관심분야/전공 설정 + 자료 보관함, `/profile/account`는 계정 정보·비밀번호 변경·구독/결제 요약·회원 탈퇴(danger zone). 새 계정 관련 기능은 `/profile/account`에, 학습 자료/과목 관련 기능은 `/profile`에 추가할 것.
- `SummaryNote.type`은 `"summary"`(요약, 항상 2단 렌더링) | `"explanation"`(강의식 설명, 항상 1단 산문) — 사용자가 고르는 "레이아웃"이 아니라 생성 자체가 다른 두 종류의 결과물. 한 파일에 대해 둘 다 독립적으로 생성 가능 (`/notes/[id]` 탭 UI, `NoteTabs.tsx`). 생성 API는 `/api/notes/[id]/generate` (구 `summarize`는 삭제됨).
- 채팅(`ChatMessage`)은 노트 상세 페이지의 탭이 아니라 **독립 메뉴** `/tutor`(목록) → `/tutor/[id]`(대화, `TutorChat.tsx`) — API는 `/api/tutor/[id]/chat` (구 `/api/notes/[id]/chat`에서 이동함, 새 API 만들 때 헷갈리지 말 것). 매 턴마다 파일 콘텐츠 블록을 대화 첫 user 메시지에 다시 첨부해서 보냄(Anthropic은 stateless) — `buildFileContentBlocks(file, {cache:true})`로 마지막 블록에 `cache_control: {type:"ephemeral"}`를 붙여 반복 전송 비용을 캐싱으로 줄임.
- 시험 생성(`/api/exams` POST)은 이제 `SummaryNote` 텍스트가 아니라 업로드된 원본 파일(`sourceFileIds`, 선택적 `pastExamFileId`+`pastExamWeight` 0~10)을 `buildFileContentBlocks`로 그대로 Claude에 전달함 — 파일마다 `[참고 자료: 파일명]`/`[기출문제: 파일명]` 텍스트 라벨을 앞에 붙여서 구분.
- **사용량 제한(`src/lib/usage.ts`)**: 세 플랜(무료/Pro/Master) 전부 한도는 "호출 횟수"가 아니라 **월간 누적 토큰**(`FREE_PLAN_MONTHLY_TOKEN_LIMIT`/`PRO_PLAN_MONTHLY_TOKEN_LIMIT`/`MASTER_PLAN_MONTHLY_TOKEN_LIMIT`, 기본 30만/106만/179만)이다 — Pro/Master도 과거엔 완전 무제한이었지만, 무제한이면 헤비 유저 1명이 월 구독료(₩8,800/₩14,800)보다 훨씬 큰 Claude 비용을 낼 수 있어 상한을 둠. Claude를 실제로 호출하는 라우트들(`notes/[id]/generate`, `exams` POST, `exams/[id]/submit`의 AI 채점 분기, `tutor/[id]/chat` POST, `scholarships/matches` GET, `profile` PUT의 대외활동 재분류 분기) 전부 인증 직후 `getQuotaStatus(userId)`로 초과 여부를 먼저 체크 → 초과 시 Claude 호출 전에 `402` 반환. `getQuotaStatus`는 별도 카운터 테이블 없이 `AiUsageEvent`(Claude 호출 1건당 1행, 원가 로그 테이블)의 이번 달 토큰을 집계해서 `used`로 쓴다 — 단, **단순 개수 합산이 아니라 원가 비율로 가중합산한다**(`getMonthlyTokenUsage`): 출력 토큰은 입력의 5배, 캐시생성은 1.25배, 캐시읽기는 0.1배(`src/lib/ai/pricing.ts`의 실제 단가 비율, 인트로→정가 전환에도 이 비율 자체는 안 바뀜). 단순 개수 합산이었을 때는 "한도를 전부 출력 토큰으로 채우는" 최악의 경우 실제 원가가 구독료를 넘어버리는 문제가 있었는데(예: Pro 100만 토큰 전부 출력 = 정가 기준 $15, 구독료는 ₩9,900≈$7), 가중합산을 쓰면 한도를 다 채웠을 때의 실제 원가가 토큰 종류와 무관하게 `한도 × 현재 입력단가`로 고정된다. Pro/Master 기본값은 이 성질을 이용해 **원가율(최악의 경우 원가 ÷ 구독료) 50%를 목표로 역산**한 값(2026-08-31 이후 정가 기준 — 인트로가로 계산하면 그 이후 원가율이 다시 올라가므로 안전하게 정가로 계산함, 환율은 1,380원/$ 근사치): Pro 106만(₩8,800÷1,380×50%≈$3.19 목표원가÷$3/M), Master 179만(₩14,800÷1,380×50%≈$5.36 목표원가÷$3/M). **2026-08-22, 실사용 중 마스터 플랜 유저가 기존 120만 한도를 초과하는 사례가 나와서** 세 플랜 모두 한도를 올리면서(20만/55만/120만 → 30만/106만/179만) 원가율도 함께 올렸다(Pro 30%→50%, Master 36%→50%, 요금도 Pro ₩7,800→₩8,800·Master ₩13,800→₩14,800) — Pro/Master 원가율을 같은 50%로 맞추면서, 예전에 있던 "Master가 Pro보다 원당 토큰을 더 후하게 줘서(볼륨 할인) 업그레이드 명분을 만드는" 차별화(Pro 원가율 30% < Master 36%)는 사라졌다(현재는 두 플랜 다 한도÷요금 ≈121토큰/원으로 동일) — 나중에 다시 차별화하려면 Master 원가율만 50%보다 높게(예: 55~60%) 잡을 것. 무료(30만)는 구독료가 없어 이 원가율 로직 대신 "무료 체험 1인당 감당 가능한 절대 비용"으로 별도로 정함 — 20만이었다가(정가 기준 최악 $0.6≈₩830) 2026-08-22에 30만으로 올림(정가 기준 최악 $0.9≈₩1,240, 자세한 계산은 `src/lib/usage.ts`의 상수 옆 주석 참고). `recordAiUsage`가 호출 직후(성공/실패 불문, 실패 시 토큰 0) 이미 원본 토큰 행을 남기므로, 별도의 "성공 시 기록" 스텝이 필요 없다(과거엔 `UsageEvent`라는 카운트 전용 모델과 `recordUsage()` 호출이 따로 있었지만 정보가 완전히 겹쳐서 삭제함). **Claude 호출을 부르는 새 라우트를 추가하면 호출 전 `getQuotaStatus` 체크 + 호출 시 `recordAiUsage` 기록 패턴을 반드시 따라야 함** — `recordAiUsage`를 빠뜨리면 그 라우트의 토큰이 다음 쿼터 계산에 안 잡혀 한도를 조용히 우회하게 됨. 관리자 계정(`isAdmin`)만 플랜과 무관하게 무제한. **PDF 영어자료 변환(DeepL)은 Claude를 호출하지 않아 `AiUsageEvent`에 행이 없으므로 이 토큰 쿼터를 전혀 소비하지 않는다** — 호출 전 `quota.allowed`(다른 기능으로 이미 한도를 넘겼는지)만 체크하고, 그 기능 자체의 빈도 제한은 페이지 수 상한(`FREE_PLAN_TRANSLATE_MAX_PAGES` 등)에만 의존한다.
- **결제/구독(토스페이먼츠)**: Stripe의 Subscription 자동 관리 기능이 없어서 반복 청구를 **직접 스케줄링**해야 함. `POST /api/billing/toss/charge-due`(`CRON_SECRET` Bearer 토큰으로 보호)가 `subscriptionStatus:"active"` && `currentPeriodEnd`가 지난 사용자를 찾아 `chargeBilling` 호출 → 성공 시 `currentPeriodEnd` 1개월 연장, 실패 시 즉시 `plan:"free"`로 다운그레이드. **이 라우트를 누군가 주기적으로 호출해줘야만** 실제로 매달 청구가 일어남(README에 수동 curl 명령 + 로컬/배포 스케줄링 안내 있음) — Stripe 때처럼 알아서 되는 게 아님, 잊지 말 것. `GET /api/billing/toss/register`(카드 등록 성공 리다이렉트 타겟)가 빌링키 발급+첫 달 결제를 한 번에 처리. `POST /api/billing/toss/cancel`은 즉시 `plan:"free"` 다운그레이드(유예기간 없음).
- **관리자**: 별도 role/permission 테이블 없이 `src/lib/admin.ts`의 `isAdmin(email)`이 `ADMIN_EMAIL` 환경변수와 단순 비교. `/admin/inquiries` 페이지는 관리자가 아니면 `notFound()`(리다이렉트 아님 — 페이지 존재 자체를 숨김).
- **PDF 영어자료 변환(`src/lib/translate/`)**: 노트/시험/AI선생님이 공유하는 `UploadedFile`(`purpose:"note"`) 풀과 완전히 분리된 별도 모델(`PdfTranslation`, 원본/번역본 PDF 경로를 각각 `originalStoredPath`/`translatedStoredPath`에 저장) — 이 기능 파일은 마이페이지 자료 보관함이나 `/notes`·`/tutor`·`/exams/new`에 노출되지 않음.
  - **번역 자체는 DeepL Document Translation API**(`deepl.ts`)가 한다 — Claude가 아님. 업로드(`POST /document`) → 상태 폴링(`POST /document/{id}`, "queued"→"translating"→"done") → 결과 다운로드(`POST /document/{id}/result`) 3단계로, PDF 파일 하나를 통째로 보내면 번역된 PDF 파일 하나를 그대로 돌려받는다. `DEEPL_API_KEY`(Free 계정은 키 끝이 `:fx`, 이 경우 `DEEPL_API_URL`은 `api-free.deepl.com`으로 고정 — Pro/Developer/Growth 유료 키는 `:fx`가 안 붙고 `api.deepl.com`을 씀)가 없으면 업로드 시점에 즉시 에러.
  - **이 방식을 택한 이유**: 이전엔 Claude vision(또는 Claude vision+`tesseract.js` OCR 하이브리드)으로 PDF를 페이지 이미지로 raster화한 뒤 그 위에 번역 텍스트 박스를 좌표를 추정해서 겹쳐 그리는 방식이었는데, 실사용 테스트에서 "번역 상자가 원문 위치를 못 맞춰서 원문이 겹쳐 보이거나 잘려서 남는" 문제가 반복적으로 재현됐다(vision 단독 방식은 문단 전체가 겹치는 수준으로 심각했고, OCR 실측 bbox로 안전망을 추가해도 아이콘/배너처럼 시각적으로 독특한 블록에서 가끔 위치가 크게 틀렸다) — 좌표를 "추측"하는 접근 자체의 근본 한계였다. DeepL Document Translation은 PDF의 실제 텍스트 레이어를 직접 읽어 문서를 재구성해서 돌려주므로 이 좌표 추측 문제 자체가 없다. **단, 텍스트 레이어가 있는 PDF(PPT/문서를 그대로 내보낸 것)에서만 잘 동작하고, 스캔 이미지로만 이루어진 PDF는 제대로 처리되지 않을 수 있다** — 이 프로젝트의 주 사용처(강의자료 PPT→PDF 변환본)에는 맞는 전제이지만, 완전히 스캔된 자료라면 품질이 떨어질 수 있는 게 알려진 한계.
  - **무료/Pro 페이지 상한(5/60, `pipeline.ts`의 `FREE_PLAN_TRANSLATE_MAX_PAGES`/`PRO_PLAN_TRANSLATE_MAX_PAGES`)을 DeepL 업로드 전에 적용**한다 — DeepL Document API는 "문서 전체"를 번역하는 API라 페이지 범위를 지정하는 파라미터가 없으므로, `pdf-lib`(`getPdfPageCount`에도 이미 쓰던 라이브러리)로 원본에서 앞쪽 N페이지만 잘라낸 새 PDF를 만들어 그것만 업로드한다(`subsetPdfPages()`). 이 상한은 노트 업로드의 `FREE_PLAN_MAX_PDF_PAGES`(40, `fileKind.ts`)와는 별개 상수.
  - **번역된 PDF 파일만 Storage에 저장하고 페이지별 이미지는 만들지 않는다** — DeepL이 페이지 단위가 아니라 문서 전체를 한 번에 돌려주므로, 뷰어(`TranslationViewer.tsx`)가 보여줄 페이지 이미지는 원본과 번역본 둘 다 그때그때 저장된 PDF에서 라스터화한다(`/api/translate/[id]/page/[pageNumber]?variant=original|translated`, `render.ts`의 `renderPdfPage`를 두 variant가 동일하게 재사용 — `@hyzyla/pdfium`으로 raw RGBA 렌더 후 `sharp`로 PNG 인코딩). 이전엔 원본만 이렇게 하고 번역본은 미리 만든 PNG를 저장했었는데, DeepL 도입 후 번역본도 같은 방식으로 통일됨.
  - 회원 탈퇴(`/api/profile/delete`)·번역 건 삭제(`DELETE /api/translate/[id]`) 시 원본 PDF와 번역된 PDF 둘 다 Storage에서 정리해야 함(`UploadedFile`과 별도 테이블이라 빠뜨리기 쉬움) — `translatedStoredPath`는 처리 완료 전엔 `null`일 수 있으니 존재할 때만 삭제.
  - 사용량 게이트(`getQuotaStatus`/`recordUsage`, `src/lib/usage.ts`)는 Claude를 호출하지 않게 된 후에도 그대로 유지한다 — 이건 "Claude 토큰 비용" 전용이 아니라 "무료 플랜 월간 AI 기능 사용 횟수" 전체를 세는 공용 카운터라서, DeepL처럼 비용이 드는 외부 API를 쓰는 한 계속 게이트해야 맞다. 다만 `recordAiUsage`(Claude 토큰/비용 로그, `AiUsageEvent`)는 이제 이 라우트에서 발생하지 않음 — DeepL 자체 비용은 별도로 추적하지 않고 있음(추후 필요하면 새로 추가).
  - **번역된 PDF의 실제 페이지 수는 업로드한 페이지 수보다 늘어날 수 있다** — 한글 번역문이 원문보다 길어지면 DeepL이 고정폭 텍스트 상자 안에 다 못 넣고 다음 페이지로 내용을 넘겨서(원본엔 없던 페이지가 새로 생김) 페이지 수가 불어나는 현상이 실측 확인됨(예: 26페이지 업로드 → 31페이지로 반환). 그래서 `pipeline.ts`는 업로드 전 페이지 수를 그대로 쓰지 않고, 다운로드한 번역 PDF를 다시 `pdf-lib`로 열어 실제 페이지 수를 센 값을 `translatedPageCount`에 저장한다 — 안 그러면 새로 생긴 뒷페이지가 뷰어에서 접근 불가능해짐. `TranslationViewer.tsx`는 원본/번역본 페이지 번호를 원래도 독립적으로 넘기고 있어서(공용 페이지 번호가 아님) 이 페이지 수 불일치 자체는 UI 쪽 추가 대응 없이도 자연스럽게 처리됨.
  - **알려진 한계 — 텍스트 색상/서식 유실**: 원문에서 빨간색·굵게 등으로 강조된 텍스트 중 일부가 번역 후 검은색 일반 텍스트로 바뀌는 경우가 실측 확인됨(같은 페이지 안에서도 어떤 강조 텍스트는 유지되고 어떤 건 유실되는 등 일관성 없음). DeepL Document Translation API 자체의 한계로 확인됨 — 업로드 API(`POST /v2/document`)에 서식(색상 등) 보존을 제어하는 파라미터가 아예 없고, DeepL 포함 AI 문서번역 도구들의 공통적으로 보고되는 한계(표·복잡한 레이아웃·글머리기호·색상 같은 문자 단위 서식이 번역 재구성 과정에서 종종 유실됨)이기도 함. 우리 쪽 `pdf-lib` 서브셋 처리를 거치지 않는 1페이지 문서에서도 재현되어 우리 코드 문제가 아님을 확인함. 텍스트 내용 자체는 항상 보존되고 색상만 사라지는 안전한 실패 모드라 사용자 확인 후 그대로 두기로 함(대안은 다시 자체 렌더링 방식으로 돌아가는 것인데, 그건 이미 겪은 "원문 위치를 못 맞추는" 훨씬 심각한 문제를 재도입하는 트레이드오프라 비권장).
  - **번역본 PDF 다운로드**: `GET /api/translate/[id]/download`가 `translatedStoredPath`를 그대로 내려준다(`Content-Disposition: attachment`, 파일명은 `{원본파일명}_번역본.pdf` — `exams/[id]/pdf` 라우트와 동일하게 `filename*=UTF-8''` 인코딩). `TranslationViewer.tsx` 상단에 다운로드 버튼으로 노출.
- **대외활동/공모전 수집(`src/lib/activity/linkareer.ts`)**: AI 웹검색이 아니라 **링커리어(linkareer.com) 직접 스크래핑**. 링커리어가 Next.js+Apollo GraphQL이라 목록(`/list/activity`, `/list/contest`)·상세(`/activity/{id}`) 페이지 HTML의 `<script id="__NEXT_DATA__">`에 완전한 구조화 JSON이 그대로 들어있어서, HTML 파싱 라이브러리 없이 그 JSON만 꺼내 씀. 목록 페이지는 이미 `status: "OPEN"`(모집중)만 내려주고, 상세 페이지의 `interests`/`targets` ref를 resolve하면 `fieldTags`/`targetInfo`용 통제 어휘 라벨을 얻음. `recruitCloseAt`(Unix ms)이 있어서 `deadlineDate`/`deadlineText`가 처음으로 실제 값을 가짐 — `syncActivityListings`(`src/lib/activity/sync.ts`)가 매 동기화 후 `deadlineDate < 오늘`인 행을 자동 삭제하므로 관리자가 수동으로 지울 필요가 없어짐. **`ActivityListing` 조회는 항상 개수를 제한할 것** — 스크래핑 전체 실행 시 DB에 1,000건 넘게 쌓이므로, 사용자 화면(`getActivitiesForUser`, `src/lib/activity/match.ts`)은 매칭 점수순 상위 60개(`MAX_DISPLAYED_ACTIVITIES`)만 반환함(관리자 페이지는 예외적으로 전체 표시).
- **장학금 데이터 수집(`src/lib/scholarship/kosaf.ts`)**: 한국장학재단이 공공데이터포털(data.go.kr)에 공개한 "학자금지원정보(대학생)" 연동. **처음엔 "오픈API"로 오해해서 공공데이터포털 활용신청(승인 대기 필요)을 전제로 스텁만 남겨뒀었지만, 실제로는 "파일데이터"로 등록되어 있어 활용신청/API키 없이 CSV를 그냥 다운로드할 수 있음**을 실제로 다운로드해서 확인함(무료, 이용허락범위 제한 없음, 월간 갱신, 1,850여 행 22개 컬럼). 다운로드는 버튼 클릭 한 번처럼 보이지만 내부적으로 `POST /tcs/dss/selectFileDataDownload.do`(다운로드용 `atchFileId`/`fileDetailSn` 조회, 요청 시 `atchFileId`는 빈 문자열로 보내는 게 정상 — 서버가 그 자리에서 채워줌) → `GET /cmm/cmm/fileDownload.do`(실제 CSV) 2단계 요청이라 그대로 재현함 — 브라우저 UI는 그 사이에 `/cmm/cmm/check-limit.json`으로 캡차 필요 여부를 체크하지만 이건 사람의 반복 클릭을 막는 UX용이라 서버 사이드에서 생략해도 정상 동작함(월 1회 배치 수준에선 문제없음을 확인). CSV는 **CP949(EUC-KR) 인코딩 + RFC4180 quoted 형식** — Node 18+ 내장 `TextDecoder('euc-kr')`로 별도 iconv 패키지 없이 디코딩되고, 파싱도 PPTX 때처럼 별도 패키지(csv-parse 등) 없이 직접 구현함(1,850행 전수 검증: 컬럼 수 불일치 0건, `모집시작일`/`모집종료일` 100% 유효한 `YYYY-MM-DD`). `ScholarshipListing.applyStartDate`/`applyEndDate`(`ActivityListing.deadlineDate`와 동일 패턴)로 저장해서 `syncScholarshipListings()`가 매 실행마다 마감 지난 행을 정리함 — **소스 자체가 월 1회만 갱신되므로 이보다 자주 동기화해도 새 정보를 더 빨리 못 얻지만, "이미 마감된 장학금이 계속 보이는" 문제는 동기화 주기와 무관하게 이 정리 로직이 항상 해결**함. `publicDataPk`(`15028252`)/`publicDataDetailPk`(`uddi:93ea274e-c626-40b9-8963-bbfadccc22ab`) 등은 이 데이터셋 페이지 다운로드 버튼 onclick에 그대로 노출된 공개 값이라 하드코딩함 — KOSAF가 데이터셋을 새로 등록하지 않는 한 안 바뀜.
  - `src/lib/scholarship/match.ts`의 `matchScholarships()`는 스텁 시절 DB가 비어있을 때 짜여서 `prisma.scholarshipListing.findMany()`로 개수 제한 없이 전체를 Claude에 보내고 있었음(실제 KOSAF 동기화 후 1,850행이 그대로 쌓이면 매 매칭 요청마다 전송돼 토큰 비용·응답 시간이 급증하는 구조) — `ActivityListing`(`MAX_DISPLAYED_ACTIVITIES=60`, `src/lib/activity/match.ts`)과 같은 이유로 `MAX_SCHOLARSHIP_CANDIDATES=150`을 두고 `applyEndDate asc(nulls last)` 정렬 후 상위 N개만 보내도록 수정함. 다만 활동 쪽처럼 점수 계산 후 자르는 게 아니라 **정렬 후 단순 `take`** — `ScholarshipListing`엔 `fieldTags` 같은 구조화된 매칭용 카테고리가 없어서(자격 요건이 자유 텍스트라 LLM 판단에 의존) 활동 쪽 점수 로직을 그대로 옮기지 못했고, 어차피 실측 후보가 20~30여 건이라 지금은 상한이 실질적으로 걸리지 않음. 후보가 실제로 150건을 넘길 정도로 늘어나면 이 캡이 마감 임박한 것부터 우선순위를 매기는 정도로만 작동하니, 그때는 진짜 사전 필터링(지역/학과 등)을 추가로 고려할 것.

## 로컬 개발

- `npm run dev` (Turbopack), `npx prisma studio`로 DB 확인 가능.
- `.env.local`의 `ANTHROPIC_API_KEY`가 비어있으면 요약/시험생성/채점 기능은 502를 반환함 (나머지 기능은 정상 동작).
